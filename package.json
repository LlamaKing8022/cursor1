{
  "name": "cube-arena",
  "version": "1.0.0",
  "description": "A spectator simulator where colored cubes bounce around an arena and fight to be the last one alive or race to the finish line.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsx tests/simulation.test.ts && tsx tests/map.test.ts && tsx tests/mechanics.test.ts && tsx tests/seedFinder.test.ts",
    "test:browser": "tsx tests/browser.check.ts"
  },
  "keywords": [
    "simulation",
    "battle-royale",
    "canvas",
    "game"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "playwright": "^1.62.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
